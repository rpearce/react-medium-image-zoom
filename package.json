{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --verbose --parallel --topological --exclude eslint-config-iz run build",
    "build:docs": "cp packages/image-zoom/dist/iife/image-zoom.* docs",
    "clean": "yarn workspaces foreach --verbose --parallel --exclude eslint-config-iz run clean",
    "contributors:add": "all-contributors add",
    "contributors:generate": "all-contributors generate",
    "coverage": "jest --coverage --coverageReporters=text-lcov | coveralls",
    "lint": "yarn workspaces foreach --verbose --parallel --exclude eslint-config-iz run lint",
    "serve:docs": "serve docs",
    "start": "yarn run build && run-p watch serve:docs",
    "test": "yarn workspaces foreach --verbose --parallel --exclude eslint-config-iz run test",
    "watch": "chokidar 'packages/*/source/**/*' -c 'yarn run build'"
  },
  "devDependencies": {
    "all-contributors-cli": "6.16.0",
    "chokidar-cli": "2.1.0",
    "coveralls": "3.1.0",
    "npm-run-all": "4.1.5",
    "serve": "11.3.2"
  }
}
